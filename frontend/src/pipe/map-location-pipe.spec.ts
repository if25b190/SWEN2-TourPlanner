import { MapLocationPipe } from './map-location-pipe';

describe('MapLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
